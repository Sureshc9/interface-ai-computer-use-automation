# Evidence status

The checked-in `replay-*` directories are real Chromium executions against the local Northstar target. They demonstrate successful deterministic extraction and a separate `MEMBER_NOT_FOUND` business outcome. All member records are synthetic.

A genuine OpenAI discovery run is intentionally not claimed here because no API credential was available in the build environment. Before submission, run the README's `hands discover` command with the applicant's API key, save its generated directory here, and add the reviewed actions as a draft/approved artifact. Do not substitute `ScriptedPlanner` output; it is test-only and would not satisfy the assignment.
